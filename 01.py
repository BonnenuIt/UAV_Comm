# from gym import spaces
# import numpy as np
# size=2
# np.array([-size, -size, -size])
# max_pos=2
# observation_space = spaces.Dict(
#             {
#                 "agent": spaces.Box(low=min_pos, high=max_pos, shape=(2,), dtype=np.float),  # The continuous observation space
#                 "target": spaces.Box(low=min_pos, high=max_pos, shape=(2,), dtype=np.float),
#             }
#         )
# print(np.random.uniform(min_pos, max_pos, size=2))
# # print(observation_space)